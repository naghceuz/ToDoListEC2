<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Not a lottery ticket</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel='stylesheet' href='/stylesheets/bootstrap3.css' />
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  </head>
  <body>


  <!--  <div class="navbar-wrapper"> -->
      <div class="container">
        <nav>
          <div class="container"> 
            <div id="navbar" class="navbar-collapse collapse">
                      <ul class="nav navbar-nav">
                        <li class="active"><a href="#">Home</a></li>
                        <li><a href="about.html">About</a></li>
                        <li><a href="#contact">Contact</a></li>
                        <li><a href="#analysis">Analysis</a></li>
                      </ul>
            </div>  
          </div>
        </nav>
      </div>  
    <!-- </div>  -->   




    <div class="container">
    <h1>You are not a lottery ticket</h1>
    <p class="lead">If you allow yourself to have more than one focus, you’ve already blinked 
    </p>
    </div>
    


    <div class="row-fluid">
    	<div class="list offset3 span6">
    	    <table class="table table-hover table-bordered">
        	<thead class="span6"><tr><th>Things Need to Finish Today</th></tr></thead>
          
                <tbody>
                    <% todos.forEach( function (todo){ %>
            		<tr>
                        <td <% if( todo.completed == true ){ %> class="completed" <%}%> > 
                            <%= todo.title %>
                        </td>

                       <td><% if ( todo.completed == false ) 
                          { %> 
                          <a href="/complete/<%= todo._id %>">Complete</a>  
                          <%}%> 
                           <% if ( todo.completed == true ) { %>
                             <a href="/uncomplete/<%= todo._id %>">Uncomplete</a>
                             <%}%>
                        </td>

                        <td>
                          <a href="/delete/<%= todo._id %>">Delete</a>
                        </td>
                    </tr>
                    <% }); %>
                </tbody>
        	</table>
    	</div>
	</div>




  <div class="row">
    <div class=" col-xs-12 col-sm-8 col-md-8 col-lg-8">
      <form action="/create" method="post">
          <div class="input-group">
          <input type="text" name="content" class="form-control" placeholder="">
          <span class="input-group-btn">
          <input type="submit" class="btn btn-default" name="todo-submit" value="Add To List"/>
          </span>
        </div>
      </form>
    </div>
  </div>





  </body>
</html>